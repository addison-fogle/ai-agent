# Copy values into your shell environment. This file is not loaded automatically.
OPENAI_API_KEY=replace-with-your-api-key
OPENAI_MODEL=gpt-5-mini
# Optional; default is storage/ next to app.py, independent of working directory.
# AGENT_STORAGE_DIR=/absolute/path/to/private/storage

